<!--
Write for someone who was not in the conversation. Say what changed and why it is correct.
Plain prose is fine. Skip the headings if they do not help.
-->

**What this changes**

**Why it is correct**

**RFC** <!-- link the RFC this implements, or say why no RFC is needed -->

---

- [ ] Commits are signed off (`git commit -s`)
- [ ] Tests cover the new behaviour, or the description explains why they cannot
- [ ] `go build ./... && go vet ./... && go test ./...` passes and `gofmt` is clean
- [ ] No unreferenced constants, helpers or fields added for later use
